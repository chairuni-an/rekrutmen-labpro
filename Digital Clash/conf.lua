-- Configuration
function love.conf(t)
	t.title = "Digital Clash" -- The title of the window the game is in (string)
	t.version = "0.10.1"
	t.window.fullscreen = true
	-- For Windows debugging
	t.console = true
end
